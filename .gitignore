# --- Student / semester data (NEVER commit) ---
*.json
!package.json
!package-lock.json

# Common export / backup names
regn-tracker.json
*-tracker.json
*semester*.json
*backup*.json

# OS / editor noise
.DS_Store
Thumbs.db
desktop.ini
*.log

# Local env / secrets (future-proof)
.env
.env.*

# Prototype files
Clinical Schedule.html

# Local mock OneDrive (FERPA / test data — never commit)
mock-onedrive/

# Design Docs
docs/Design Docs/

# Node / Vite build
node_modules/
dist/