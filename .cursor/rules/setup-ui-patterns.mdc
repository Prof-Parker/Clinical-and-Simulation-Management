---
description: Setup and list UI patterns — add/remove rows, checkboxes, dialogs, UI registry smoke tests
globs: index.html,src/ui/**,src/ui/ui-registry.js,tests/ui-smoke.test.js,css/app.css
alwaysApply: false
---

# Setup UI Patterns

Follow these conventions when adding or changing Setup tab UI (and similar list editors elsewhere).

## Adding future UI

When you add a tab, panel, or control:

1. Add markup to `index.html`
2. Register it in [`src/ui/ui-registry.js`](src/ui/ui-registry.js) — tab + `anchors`, or `UI_SHELL` / `UI_MENU` / `UI_MODALS`
3. Wire behavior in `main.js` or the feature module
4. Run `npm test` — [`tests/ui-smoke.test.js`](tests/ui-smoke.test.js) failures point at missing ids or broken routing

Registry contract (see comments in `ui-registry.js`):

- **Tabs** — `id` must match `.nav-tab[data-tab]` and `#view-{id}`; list key controls in `anchors`
- **Shell** — header, banners, global chrome (`UI_SHELL`)
- **Menu** — dropdown actions wired in `initUI` (`UI_MENU`)
- **File inputs** — hidden import pickers (`UI_FILE_INPUTS`)
- **Modals** — gate, config, dialog controls (`UI_MODALS`)

Example for a new tab:

```javascript
{
  id: 'reports',
  anchors: ['reportsTable', 'reportsExportBtn']
}
```

Also register the tab id in [`src/auth/user-template.js`](src/auth/user-template.js) for roles that should see it. The smoke test checks that every role tab is listed in the registry.

## Add buttons — bottom of list

Place **Add** actions at the **bottom** of the list container, not in section headers.

```html
<div id="exampleList">
  <!-- row markup -->
  <div class="config-list-add-row">
    <button type="button" class="btn btn-sm add-example">Add item</button>
  </div>
</div>
```

- Container class: `config-list-add-row` (use `clin-groups-add-row` too when inside a CSS grid that needs `grid-column: 1 / -1`).
- Button: `btn btn-sm` plus a semantic action class (e.g. `add-holiday`, `add-facility`, `add-site-lib`, `add-clin-group`).
- Render the add row in JS after list rows so it stays on the last line as items grow.
- Wire clicks with **delegated** listeners on the list container (`e.target.closest('.add-example')`), not `getElementById` on a re-rendered button.
- Disable during closeout via `#view-setup .config-list-add-row .btn` in `updateReadOnlyButtons`.

Reference: `renderClinicalGroupsList`, `renderHolidays`, `renderFacilities`, `renderSiteLibrary`, `renderSections`, `renderOrientations`, `renderSimDaysList`.

## Remove buttons

```html
<button type="button" class="btn btn-icon-remove remove-example"
  aria-label="Remove example" title="Remove example">&times;</button>
```

- Always include `aria-label` and `title`.
- Use delegated click on the list container with `e.target.closest('.remove-example')`.
- When a minimum of one row is required, show a muted `Min. 1` hint instead of a remove button.
- For select-based rows (facilities, sim days, clinical groups), use `config-list-row` so the control and remove button share one grid row.

## Dropdowns (select)

Use the shared `.select-control` class for toolbar and filter dropdowns (header course picker, dashboard filters, and similar compact selects outside full-width form layouts).

```html
<select class="select-control" aria-label="Filter by clinical group">…</select>
```

- Tokens: `background: var(--bg)`, `color: var(--text)`, `border: 1px solid var(--border)` — works in light and dark mode via `html.dark` CSS variables.
- Do **not** use undefined tokens such as `var(--card)` or `var(--surface)` on selects.
- Full-width form fields inherit global `input, select, textarea` styles (`width: 100%`); add `select-control` when `width: auto` is needed.
- Dashboard filter selects inside `.filters` / `.dashboard-schedule-filters` already match this pattern; new header/toolbar selects should use `select-control` explicitly.
- Always set `aria-label` (or an associated `<label>`).

## Checkboxes

Use `label.filter-check` for all checkbox labels (filters, toggles, tag pickers):

```html
<label class="filter-check site-lib-tag">
  <input type="checkbox" data-site-lib-tag="MS" checked> MS
</label>
```

- Do not style raw `<input type="checkbox">` without the `filter-check` wrapper.
- Checkbox sizing and accent color come from `label.filter-check input[type="checkbox"]` in `css/app.css`.
- Optional compact variant: `filter-check filter-check-compact` (dashboard filters).

## Dialogs — not native alert/confirm

Use `#dialogModal` helpers from [`src/ui/dialogs.js`](src/ui/dialogs.js):

| Need | API |
|------|-----|
| OK only | `showAlert(title, message, onOk?)` |
| Confirm / cancel | `showConfirm(title, message, onConfirm, options?)` |
| Custom form body | `showDialog(title, bodyHtml, onSave)` |

For edit gating (roles + audit closeout), use `guardEditable(action)` from [`src/auth/permissions.js`](src/auth/permissions.js).

Never use `window.alert`, `window.confirm`, or `window.prompt`.

- Confirm destructive actions (remove student, regenerate schedules) with `showConfirm`.
- Informational outcomes use `showAlert`.
- Complex single-field or multi-control edits use `showDialog` (see master calendar cell editor).

## Read-only and auto-filled form fields

Use `.setup-autofill-field` for inputs populated from registry or other sources and not edited directly (e.g. lead faculty email after picking a user).

```html
<input type="text" id="leadFacultyEmail" class="setup-autofill-field" readonly aria-label="Lead faculty email">
```

- **Always set both** `background` and `color` using theme tokens — never a hard-coded light gray alone.
- Required tokens (defined in `:root` and `html.dark` in `css/app.css`):
  - `background: var(--surface-alt)`
  - `color: var(--text)`
  - `border-color: var(--border)`
- Do **not** use `var(--surface-alt, #f3f4f6)` or other light-only fallbacks; undefined tokens silently break dark mode (light text on a light background).
- Set `readonly` in JS when the value is registry-driven; remove the class and `readonly` when the field becomes editable again.
- Prefer `type="text"` over `type="email"` for auto-filled values that may not be full addresses (avoids browser validation styling quirks).

Reference: `#leadFacultyEmail` in Setup → Course Staff (`src/ui/setup/facilities-faculty.js` → `renderLeadFaculty`).

## Section headers

Section titles live in headers **without** add buttons when the list uses the bottom add-row pattern. Keep explanatory copy in `section-sub` paragraphs above the list.
