/**
 * Theory tab placeholder.
 */

function render() {
    var el = document.getElementById('theoryStubPanel');
    if (!el) return;
    el.innerHTML = '<p class="section-sub">Theory course and adjunct scheduling features are planned for a future release.</p>';
  }

  function init() {}

export {
  init,
  render
};
